---
interface Props {
  number: string;
  headline: string;
  body: string;
}

const { number, headline, body } = Astro.props;
---

<div class="flex gap-5 bg-white dark:bg-surface border-l-4 border-success dark:border-navy rounded-r-lg p-6 shadow-sm dark:shadow-none">
  <span class="text-4xl font-bold text-success dark:text-navy-light shrink-0 leading-none" style="font-family: var(--font-display);">
    {number}
  </span>
  <div>
    <h3 class="text-lg font-bold text-ink mb-1" style="font-family: var(--font-sans);">{headline}</h3>
    <p class="text-muted text-sm leading-relaxed">{body}</p>
  </div>
</div>
